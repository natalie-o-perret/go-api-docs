version: "2"
linters:
  default: standard
  enable:
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - govet
  exclusions:
    rules:
      - path: "^examples/"
        text: "."
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
